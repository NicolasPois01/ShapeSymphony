export interface Circle {
  id: number,
  x: number,
  y: number,
  xSpeed: number,
  ySpeed: number,
  color: string,
  startX: number,
  startY: number,
  instrument: string,
  note: string,
  alteration: number,
  octave: number,
  spawnTime: number,
  maxBounces: number,
  maxTime: number
}
