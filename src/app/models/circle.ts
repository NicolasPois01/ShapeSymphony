export interface Circle {
  id: number,
  x: number,
  y: number,
  xSpeed: number,
  ySpeed: number,
  color: string,
  startX: number,
  startY: number,
  instrument: string,
  note: string,
  spawnTime: number,
  maxBounces: number,
  maxTime: number
}
